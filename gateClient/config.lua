local data = {
  -- the address of the control server
  address = "0.0.0.0",
  -- the port of the control server
  port = 123456,
  -- the name of this gate
  name = "gateName",
  -- how many tries to give up after (set to false or nil to not give up)
  giveUpAfterNTries=false
}
return data